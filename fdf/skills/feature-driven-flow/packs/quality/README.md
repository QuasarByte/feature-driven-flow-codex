# Pack: quality

Engineering and testing discipline rules.

Adds:

1. `engineering-principles`
2. `test-strategy-policy` + `test-strategy-gate.md`

How to enable:

1. Add `quality` to `packs.enabled` in `.codex/feature-driven-flow/settings.json`.
2. Optionally select the `quality-overlay` profile.
