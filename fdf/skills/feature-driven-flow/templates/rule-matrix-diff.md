# Rule Matrix Change Diff (Template)

Use when the rule matrix changes after Scope.

## Context

1. Why is the matrix changing now (what new info triggered the change)?
2. Which phase are we currently in?
3. Does `AGENTS.md` impose constraints that require this change?

## Before (Confirmed In Scope)

Formatting:
1. Keep one row per phase.
2. In "Rules", use backticked rule ids separated by commas.
3. If this matrix is echoed in terminal/chat, do not render a table; use only a plain-text phase list.

| Phase | Rules |
| --- | --- |
| scope | |
| explore | |
| clarify | |
| architect | |
| implement | |
| verify | |
| summarize | |

## After (Proposed)

Formatting:
1. Keep one row per phase.
2. In "Rules", use backticked rule ids separated by commas.
3. If this matrix is echoed in terminal/chat, do not render a table; use only a plain-text phase list.

| Phase | Rules |
| --- | --- |
| scope | |
| explore | |
| clarify | |
| architect | |
| implement | |
| verify | |
| summarize | |

## Diff Summary

1. Added rules: `<id...>`
2. Removed rules: `<id...>`
3. Rationale per change: `<short bullets>`

## Approval

Please confirm the updated rule matrix before continuing.

