Explain current effective instructions provenance and status.

Task:
$ARGUMENTS

Requirements:
1. Identify whether current instructions are:
   - imported from bundle,
   - imported from compact file,
   - rebuilt from current assets.
2. Show source path(s), validation status, and disposition decision captured in Scope.
3. Summarize linked matrix provenance (`selected_profiles`, `profile_overrides`, `enabled_packs`).
4. If `custom_instructions` exists, summarize decision/approval state and approved/draft/rejected counts.
5. Highlight staleness risk indicators when source asset versions differ from current repo state.
