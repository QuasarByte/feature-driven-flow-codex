Preview Scope candidates before confirmation.

Task:
$ARGUMENTS

Requirements:
1. Gather current Scope candidate set:
   - effective matrix candidate
   - compiled instructions candidate (bundle/compact) if provided
   - profile-based recommended compilation
2. Validate candidates and identify conflicts/mismatches.
3. Present a concise preview:
   - what will be used if accepted
   - what differs from recommendation
   - risks and tradeoffs
4. End with explicit disposition options:
   - accept candidate
   - adjust candidate
   - discard and use recommendation
